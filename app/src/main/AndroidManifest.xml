<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.marco.marplex.schoolbook">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.marco.marplex.schoolbook.LoginActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.marco.marplex.schoolbook.DashboardActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme" />
        <activity
            android:name="com.marco.marplex.schoolbook.Materia"
            android:label="@string/title_activity_materia"
            android:screenOrientation="portrait"
            android:theme="@style/AppThemeGreen" />
        <activity
            android:name="com.marco.marplex.schoolbook.CompareActivity"
            android:label="@string/title_activity_compare"
            android:screenOrientation="portrait"
            android:theme="@style/AppThemeOrange" />

        <service android:name="com.marco.marplex.schoolbook.services.NotificationService" />

        <activity android:name="com.marco.marplex.schoolbook.SettingsActivity"></activity>
    </application>

</manifest>